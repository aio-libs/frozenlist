name: python_-m_pip_install_frozenlist
on: [pull_request, push]
jobs:
  python_-m_pip_install_frozenlist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: 3.11-dev
      - run: pip install frozenlist
